# create a function that adds two numbers
def add(a, b):
    return a + b

# find all strings that contain the word "test"
def find_test_strings(strings):
    return [s for s in strings if "test" in s]  # list comprehension

    

